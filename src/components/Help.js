import React from 'react'
import './Help.css'

const Help = (props) => (

    <section className={`${typeof (props.className) !== 'undefined' ? props.className : ''} Help`}>
            Help
    </section>
)

export default Help